global:
  message: Welcome to production cutomer service
server:
  port: 8081
message: Production cutomer service
spring:
  application:
    name: customer-service-prod
  datasource:
    url: jdbc:mysql://localhost:3306/customer_prod
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql : true
 
